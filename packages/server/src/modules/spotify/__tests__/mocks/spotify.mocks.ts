export const spotifyServiceMock = {
  login: jest.fn(),
  loginCallback: jest.fn(),
  token: jest.fn(),
  logout: jest.fn(),
  getPlaylist: jest.fn(),
  getPlaylistTracks: jest.fn(),
  playTrack: jest.fn(),
};
